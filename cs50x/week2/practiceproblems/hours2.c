#include <cs50.h>
#include <stdio.h>

int main (void)
{

    int weeks
    do
    {
        weeks = get_int ("Enter the number of weeks you've been taking CS50: ");
    }
    while (weeks < 1);

    int hours[weeks]; // ask the question for all number of weeks taken
    do
    {
        hours = get_int ("Enter the number of hours you spent on week: %")
    }
}